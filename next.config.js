/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [], // tambahkan domain jika menggunakan gambar eksternal
  },
};

module.exports = nextConfig;